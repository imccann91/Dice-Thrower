package com.gmail.imccannascii.dice;

import android.app.Activity;
import android.os.Bundle;
import android.widget.ArrayAdapter;
import android.widget.Spinner;
import java.lang.String;

/**
 * Created by Ian on 8/1/2016.
 */
/*public class DiceSelection extends Activity {

    private String[] spinnerItems;

    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        this.spinnerItems = new String[]{"d4", "d6", "d8", "d10", "d20", "d30", "d100"};

        Spinner s = (Spinner) findViewById(R.id.typeOfDiceSelection);
        ArrayAdapter<String> adapter = new ArrayAdapter<String>(this, android.R.layout.simple_spinner_item, spinnerItems);
        s.setAdapter(adapter);
    }
}*/
